apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: clawdb-data
  namespace: clawdb
  labels:
    app.kubernetes.io/name: clawdb
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
